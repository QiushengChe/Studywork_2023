import numpy as np
import matplotlib.pyplot as plt


def generate_matrix(N, p):
    matrix = np.zeros((N, N))
    return matrix


if __name__ == '__main__':
    # initial
    N = 1e2  # number of nodes, 1e2 / 1e3 / 2e3
    p = 1e-5  # possibility of connect between two nodes
    # generate nodes and edge

    # plot figure and save data
